# Gradle / Android build output
.gradle/
build/
app/build/
local.properties

# Wrapper jar is generated by the CI step (gradle wrapper)
# Keep wrapper scripts if you add them locally, but the workflow regenerates them.

# IDE
.idea/
*.iml
.DS_Store
